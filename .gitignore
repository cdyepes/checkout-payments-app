node_modules/
dist/
build/
coverage/
.env
.env.*.local
*.log
.DS_Store
.firebase/
.turbo/
*.tsbuildinfo

# test brief and scratch notes — never enter the public repo.
# Root-level markdown is ignored by default (planning/scratch files land here),
# with an explicit allowlist for the docs that are meant to be public.
*.pdf
reasons.txt
/*.md
!/README.md
!/ROADMAP.md
