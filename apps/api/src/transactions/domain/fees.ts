// Flat fees in cents (COP × 100, matching the seed data and the payment provider's
// amount_in_cents convention). Revisit if the business ever needs tiered/percentage fees.
export const BASE_FEE_IN_CENTS = 500_000; // 5,000 COP
export const DELIVERY_FEE_IN_CENTS = 800_000; // 8,000 COP
