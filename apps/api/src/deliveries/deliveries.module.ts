import { Module } from '@nestjs/common';

// Ports and entity are in place; repository adapter, use cases and controller
// land in the checkout iteration, following the products module as a template.
@Module({})
export class DeliveriesModule {}
